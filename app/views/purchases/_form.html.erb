<%= form_for(@purchase, html: {class: 'nice'}) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, placeholder: 'What did you purchase?', class: 'input-text' %>
  </div>
  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount, placeholder: 'Enter a numerical value', class: 'input-text' %>
  </div>
  <!--
  <div class="field">
    <%= f.label :user %>
    <%= f.collection_select(:user_id, User.all, :id, :name, 
    	{:prompt => true}, 
    	{"style" => "width:200px"})%>
  </div>
  -->
  <div class="actions"><p>
    <%= f.submit class: 'nice medium radius blue button'%></p>
  </div>
<% end %>
